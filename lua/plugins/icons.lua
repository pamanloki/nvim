return {
  "nvim-tree/nvim-web-devicons",
  lazy = true,
  opts = {
    -- Nerd Font v3 codepoints. If icons render as boxes, your terminal
    -- font is likely an older Nerd Font (v2) or not a Nerd Font at all.
    default = true,
    color_icons = true,
  },
}
